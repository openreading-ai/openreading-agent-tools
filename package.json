{
  "name": "openreading-agent-tools-repository",
  "version": "0.0.0",
  "private": true,
  "description": "Repository checks for OpenReading agent tooling proposals",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "verify": "npm run lint && npm test && npm run check:repo && npm run check:specs",
    "lint": "markdownlint-cli2",
    "test": "node --test tests/repository.test.mjs",
    "check:repo": "node scripts/check-repository.mjs",
    "check:specs": "productspec validate product/specs/local-document-proof.product-spec.md"
  },
  "overrides": {
    "markdownlint-cli2": {
      "smol-toml": "1.7.1"
    }
  },
  "devDependencies": {
    "@productspec/parser": "0.27.0",
    "markdown-it": "15.0.2",
    "markdownlint-cli2": "0.23.2",
    "yaml": "2.9.1"
  }
}
